---
import {getEntry} from "astro:content"
import AuthorLayout from "@/layouts/AuthorLayout.astro";

const author = await getEntry('authors', 'tropic')
const {Content} = await author.render();
---

<AuthorLayout content={author.data}>
	<Content />
</AuthorLayout>
